{{!--
  Age Gate Component Template - Luxury Velvet Cinema Aesthetic
  Redesigned with frontend-design plugin for exceptional UX/UI
--}}

<style>
  /* Global Age Gate Styles */
  .age-gate-wrapper {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 20px;
    position: relative;
    overflow: hidden;
    background: linear-gradient(135deg, #1a0a2e 0%, #3d1652 50%, #5c1a51 100%);
    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
  }

  /* Animated Background Elements */
  .age-gate-wrapper::before {
    content: '';
    position: absolute;
    top: -50%;
    left: -50%;
    width: 200%;
    height: 200%;
    background: radial-gradient(circle, rgba(219, 39, 119, 0.15) 0%, transparent 70%);
    animation: pulseGlow 8s ease-in-out infinite;
  }

  @keyframes pulseGlow {
    0%, 100% { transform: translate(0, 0) scale(1); opacity: 0.5; }
    50% { transform: translate(10%, 10%) scale(1.1); opacity: 0.3; }
  }

  /* Main Card */
  .age-gate-card {
    position: relative;
    max-width: 580px;
    width: 100%;
    background: rgba(15, 5, 25, 0.85);
    backdrop-filter: blur(20px);
    border-radius: 24px;
    border: 1px solid rgba(219, 39, 119, 0.2);
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.5),
                0 0 40px rgba(219, 39, 119, 0.15),
                inset 0 1px 0 rgba(255, 255, 255, 0.05);
    overflow: hidden;
    animation: cardEntry 0.8s cubic-bezier(0.16, 1, 0.3, 1) forwards;
    opacity: 0;
    transform: translateY(30px) scale(0.95);
  }

  @keyframes cardEntry {
    to {
      opacity: 1;
      transform: translateY(0) scale(1);
    }
  }

  /* Decorative Top Border */
  .age-gate-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 4px;
    background: linear-gradient(90deg, #db2777, #f59e0b, #db2777);
    background-size: 200% 100%;
    animation: shimmer 3s linear infinite;
  }

  @keyframes shimmer {
    to { background-position: 200% 0; }
  }

  /* Header Section */
  .age-gate-header {
    padding: 48px 40px 32px;
    text-align: center;
    border-bottom: 1px solid rgba(219, 39, 119, 0.15);
    animation: fadeInUp 1s ease 0.2s both;
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .age-gate-icon {
    font-size: 64px;
    background: linear-gradient(135deg, #db2777 0%, #f59e0b 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin-bottom: 20px;
    animation: iconPulse 2s ease-in-out infinite;
  }

  @keyframes iconPulse {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.05); }
  }

  .age-gate-title {
    font-family: Georgia, "Times New Roman", Times, serif;
    font-size: 42px;
    font-weight: 900;
    line-height: 1.2;
    margin: 0 0 12px;
    background: linear-gradient(135deg, #ffffff 0%, #f0abfc 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    letter-spacing: -0.02em;
  }

  .age-gate-subtitle {
    font-size: 16px;
    color: rgba(255, 255, 255, 0.6);
    font-weight: 300;
    letter-spacing: 0.05em;
    text-transform: uppercase;
  }

  /* Body Section */
  .age-gate-body {
    padding: 32px 40px;
    animation: fadeInUp 1s ease 0.4s both;
  }

  /* Alert Box */
  .age-gate-alert {
    background: linear-gradient(135deg, rgba(219, 39, 119, 0.1) 0%, rgba(245, 158, 11, 0.05) 100%);
    border: 1px solid rgba(219, 39, 119, 0.3);
    border-radius: 16px;
    padding: 24px;
    margin-bottom: 28px;
    position: relative;
    overflow: hidden;
  }

  .age-gate-alert::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 4px;
    height: 100%;
    background: linear-gradient(180deg, #db2777, #f59e0b);
  }

  .age-gate-alert-icon {
    font-size: 32px;
    color: #f59e0b;
    margin-bottom: 12px;
    display: inline-block;
    animation: alertBounce 1s ease 0.6s;
  }

  @keyframes alertBounce {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-5px); }
  }

  .age-gate-alert h5 {
    font-family: Georgia, "Times New Roman", Times, serif;
    font-size: 24px;
    font-weight: 700;
    color: #ffffff;
    margin: 0 0 12px;
  }

  .age-gate-alert p {
    font-size: 15px;
    line-height: 1.6;
    color: rgba(255, 255, 255, 0.8);
    margin: 0 0 12px;
  }

  .age-gate-alert hr {
    border: none;
    border-top: 1px solid rgba(219, 39, 119, 0.2);
    margin: 16px 0;
  }

  .age-gate-alert strong {
    color: #f59e0b;
    font-weight: 700;
  }

  .age-gate-alert small {
    color: rgba(255, 255, 255, 0.5);
    font-size: 13px;
  }

  /* Checkbox Section */
  .age-gate-checkbox {
    background: rgba(255, 255, 255, 0.03);
    border: 2px solid rgba(219, 39, 119, 0.2);
    border-radius: 12px;
    padding: 20px;
    margin-bottom: 24px;
    transition: all 0.3s ease;
    cursor: pointer;
    animation: fadeInUp 1s ease 0.6s both;
  }

  .age-gate-checkbox:hover {
    background: rgba(255, 255, 255, 0.05);
    border-color: rgba(219, 39, 119, 0.4);
    transform: translateY(-2px);
  }

  .age-gate-checkbox.checked {
    background: rgba(219, 39, 119, 0.1);
    border-color: #db2777;
  }

  .age-gate-checkbox input[type="checkbox"] {
    width: 24px;
    height: 24px;
    margin-right: 16px;
    cursor: pointer;
    accent-color: #db2777;
  }

  .age-gate-checkbox label {
    display: flex;
    align-items: center;
    cursor: pointer;
    margin: 0;
    font-size: 16px;
    line-height: 1.5;
    color: #ffffff;
  }

  .age-gate-checkbox strong {
    color: #f59e0b;
    font-weight: 700;
  }

  /* Error Message */
  .age-gate-error {
    background: rgba(239, 68, 68, 0.1);
    border: 1px solid rgba(239, 68, 68, 0.3);
    border-radius: 12px;
    padding: 16px;
    margin-bottom: 24px;
    color: #fca5a5;
    font-size: 14px;
    display: flex;
    align-items: center;
    gap: 12px;
    animation: shake 0.5s ease;
  }

  @keyframes shake {
    0%, 100% { transform: translateX(0); }
    25% { transform: translateX(-10px); }
    75% { transform: translateX(10px); }
  }

  .age-gate-error i {
    font-size: 20px;
  }

  /* Legal Links */
  .age-gate-legal {
    text-align: center;
    padding: 20px 0;
    font-size: 13px;
    color: rgba(255, 255, 255, 0.5);
    animation: fadeInUp 1s ease 0.8s both;
  }

  .age-gate-legal a {
    color: #f59e0b;
    text-decoration: none;
    font-weight: 600;
    transition: all 0.3s ease;
    border-bottom: 1px solid transparent;
  }

  .age-gate-legal a:hover {
    color: #ffffff;
    border-bottom-color: #f59e0b;
  }

  /* Footer Buttons */
  .age-gate-footer {
    padding: 32px 40px;
    display: flex;
    gap: 16px;
    border-top: 1px solid rgba(219, 39, 119, 0.15);
    animation: fadeInUp 1s ease 1s both;
  }

  .age-gate-btn {
    flex: 1;
    padding: 18px 32px;
    border: none;
    border-radius: 12px;
    font-size: 16px;
    font-weight: 700;
    letter-spacing: 0.03em;
    text-transform: uppercase;
    cursor: pointer;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 12px;
    position: relative;
    overflow: hidden;
  }

  .age-gate-btn i {
    font-size: 20px;
    transition: transform 0.3s ease;
  }

  .age-gate-btn:hover i {
    transform: scale(1.1);
  }

  .age-gate-btn::before {
    content: '';
    position: absolute;
    top: 50%;
    left: 50%;
    width: 0;
    height: 0;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.1);
    transform: translate(-50%, -50%);
    transition: width 0.6s, height 0.6s;
  }

  .age-gate-btn:hover::before {
    width: 300px;
    height: 300px;
  }

  .age-gate-btn-exit {
    background: rgba(255, 255, 255, 0.05);
    color: rgba(255, 255, 255, 0.7);
    border: 1px solid rgba(255, 255, 255, 0.1);
  }

  .age-gate-btn-exit:hover {
    background: rgba(255, 255, 255, 0.08);
    color: #ffffff;
    transform: translateY(-2px);
    box-shadow: 0 8px 16px rgba(0, 0, 0, 0.3);
  }

  .age-gate-btn-enter {
    background: linear-gradient(135deg, #db2777 0%, #f59e0b 100%);
    color: #ffffff;
    box-shadow: 0 8px 24px rgba(219, 39, 119, 0.4);
    position: relative;
  }

  .age-gate-btn-enter:hover {
    transform: translateY(-2px);
    box-shadow: 0 12px 32px rgba(219, 39, 119, 0.6);
  }

  .age-gate-btn-enter:disabled {
    opacity: 0.4;
    cursor: not-allowed;
    transform: none;
  }

  /* Mobile Responsive */
  @media (max-width: 768px) {
    .age-gate-header {
      padding: 36px 24px 24px;
    }

    .age-gate-title {
      font-size: 32px;
    }

    .age-gate-icon {
      font-size: 48px;
    }

    .age-gate-body {
      padding: 24px;
    }

    .age-gate-footer {
      padding: 24px;
      flex-direction: column;
    }

    .age-gate-btn {
      width: 100%;
    }
  }
</style>

<div class="age-gate-wrapper">
  <div class="age-gate-card">
    {{!-- Header --}}
    <div class="age-gate-header">
      <div class="age-gate-icon">
        <i class="fa fa-shield-alt"></i>
      </div>
      <h1 class="age-gate-title">OFINDER</h1>
      <p class="age-gate-subtitle">Verifica Età • Accesso Riservato 18+</p>
    </div>

    {{!-- Body --}}
    <div class="age-gate-body">
      {{!-- Alert AGCOM --}}
      <div class="age-gate-alert">
        <div class="age-gate-alert-icon">
          <i class="fa fa-exclamation-triangle"></i>
        </div>
        <h5>Contenuto per Adulti</h5>
        <p>
          Questo sito contiene materiale per adulti e non è adatto a minori di
          18 anni. Per accedere, devi confermare di aver compiuto la maggiore
          età.
        </p>
        <hr />
        <p class="mb-0">
          L'accesso è riservato esclusivamente a utenti maggiorenni in
          conformità alla normativa italiana.
        </p>
      </div>

      {{!-- Checkbox Accettazione --}}
      <div class="age-gate-checkbox {{if this.accepted 'checked'}}">
        <label>
          <input
            type="checkbox"
            checked={{this.accepted}}
            {{on "change" this.toggleAccepted}}
          />
          <span>
            <strong>Confermo di avere almeno 18 anni</strong>
            e di voler accedere a contenuti destinati a un pubblico adulto.
          </span>
        </label>
      </div>

      {{!-- Error Message --}}
      {{#if this.errorMessage}}
        <div class="age-gate-error">
          <i class="fa fa-times-circle"></i>
          <span>{{this.errorMessage}}</span>
        </div>
      {{/if}}

      {{!-- Legal Links --}}
      <div class="age-gate-legal">
        Accedendo al sito accetti i nostri
        <a href="#" {{on "click" this.goToTerms}}>Termini di Servizio</a>
        e la
        <a href="#" {{on "click" this.goToPrivacyPolicy}}>Privacy Policy</a>.
      </div>
    </div>

    {{!-- Footer Buttons --}}
    <div class="age-gate-footer">
      <button
        type="button"
        class="age-gate-btn age-gate-btn-exit"
        {{on "click" this.exitSite}}
      >
        <i class="fa fa-times"></i>
        <span>Esci</span>
      </button>
      <button
        type="button"
        class="age-gate-btn age-gate-btn-enter"
        disabled={{not this.accepted}}
        {{on "click" this.confirmAge}}
      >
        <i class="fa fa-check"></i>
        <span>Entra (18+)</span>
      </button>
    </div>
  </div>
</div>
